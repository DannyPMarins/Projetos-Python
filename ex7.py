import random

x = int(input('INFORME UM NÚMERO: '))

def sorteiaDezNumeros():
    lista = []
    for i in range(x)
        aux = random.randit(0,100)
        lista.append(aux)

    print(lista)
    return (lista / x )
